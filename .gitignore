# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# production
dist/

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env — never commit real secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# database (local dev only)
prisma/dev.db
prisma/dev.db-journal
*.sqlite
*.sqlite3

# uploaded files (dev local storage lives outside the public web root)
/storage/
!storage/.gitkeep

# test / coverage
coverage/
.vitest/

# typescript
*.tsbuildinfo

# editors
.vscode/
.idea/

# vercel cli (adds .vercel and .env* on `vercel link`)
.vercel
.env*
# ...but the template carries no secrets and must stay in the repo.
!.env.example
